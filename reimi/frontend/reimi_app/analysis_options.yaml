include: package:flutter_lints/flutter.yaml

linter:
  rules:

# Analysisからfreezedが生成したコードを除外する設定
analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    invalid_annotation_target: ignore
